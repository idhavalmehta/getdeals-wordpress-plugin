( function( $ ) {
	'use strict';

	var credentials = {
		'email': '',
		'token': '',
	};

	$( function() {
	
	} );
	
	$( window ).load( function() {
	
	} );

	window.getdeals = function( email, token ) {

	};

} )( jQuery );
